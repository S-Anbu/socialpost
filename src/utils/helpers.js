
// Generate unique ID (for demo purposes)
export const generateUniqueId = () => Date.now();

// Format date for posts and comments
export const formatDate = (timestamp) => new Date(timestamp).toLocaleString();
